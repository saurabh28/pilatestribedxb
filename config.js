/* Fill these in from your Supabase project, then redeploy.
   Where to find them: Supabase dashboard → your project → Project Settings
   (gear icon) → API → "Project URL" and "anon public" key.

   The anon key is meant to be public — it's safe to ship in this file.
   It cannot read or write anything on its own; every table's Row Level
   Security policy (see supabase/schema.sql) is what actually enforces that
   a trainer can only ever see their own clients. */

window.SUPABASE_URL = "https://otwuisvpbpecknnysviz.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_OsByYkLMM2MmuBsyoufReA_JmKCsHMz";
